Scanner
- Scan comments

Parser
- Unary exprs
	dereference
	slice (asd[123..123])
	index (a[12])
	array type ([]Something)

- Binary exprs
	addition / subtraction
	multiplication / division
	modulo
	logic && and ||

- Statements
	if / else
	for
	break
	continue
	defer

Analyzer
	var decl & assign cannot accept compile time expressions
	type hashing for fast type checking
	force return statements
	only allow one function called main across all files
	check if main function exists

Codegen
