Scanner
- Scan comments
- Scan c strings

Parser
- Unary exprs
	dereference
	slice (asd[123..123])
	index (a[12])
	array type ([]Something)

- Binary exprs
	addition / subtraction
	multiplication / division
	modulo
	logic && and ||

- Statements
	if / else
	for
	return
	break
	continue


Analyzer
	var decl & assign cannot accept compile time expressions
	type hashing for fast type checking
	force return statements

Codegen
	add function parameter values
