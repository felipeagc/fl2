Parser
- Unary exprs
	dereference
	slice (asd[123..123])
	index (a[12])
	array type ([]Something)

- Binary exprs
	addition / subtraction
	multiplication / division
	modulo
	logic && and ||

- Statements
	if / else
	for
	return
	break
	continue


Analyzer
	make get_expr_sym function to replace some uses of symbol_check_expr
	check if expression is valid type
	type checking
	

