Parser
- Unary exprs
	dereference
	slice (asd[123..123])
	index (a[12])
	array type ([]Something)

- Binary exprs
	addition / subtraction
	multiplication / division
	modulo
	logic && and ||

- Statements
	if / else
	for
	return
	break
	continue


Analyzer
	operand block vs operation block
	automatic numeric casting
	typeof(struct {}) == type
	print expected and received types in type check errors
	type check proc call parameters
	

