hello :: import "other.fl";
using import "entity.fl";

MyStruct :: struct {

}

main :: proc (a: i32, b: i32) {
  bb := (hello.puts())();

  aa := Entity;

  hello.puts = 123;

  derp :: proc () {
    aaa :: hello.puts;
    b := hello.puts;
  }
}
