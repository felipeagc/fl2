hello :: import "other.fl";
using import "entity.fl";

MyStruct :: struct {

}

asd :: proc (c: i32) {
  d := 123;

}

main :: proc (a: asdasdasd, b: i32) {
  bb := (hello.puts())();

  aa := Entity;


  a: i32 = 123;

  hello.puts = 123;

  derp :: proc () {
    aaa :: hello.puts;
    b := hello.puts();

    aaaa := derp();
  }

  aaa := derp();
}
