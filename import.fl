hello :: import "other.fl";
using import "hello.fl";

MyStruct :: struct {

}

main :: proc () {
  b := (hello.puts())();
  b := hello.puts;

  hello.puts = 123;

  derp :: proc () {
    a :: hello.puts;
    b := hello.puts;
  }
}
