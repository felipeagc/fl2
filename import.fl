hello :: import "other.fl";

MyStruct :: struct {

}

main :: proc () {
  b := (hello.puts())();

  using import "entity.fl";
  aa := Entity;

  hello.puts = 123;

  derp :: proc () {
    a :: hello.puts;
    b := hello.puts;
  }
}
