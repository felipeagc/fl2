using import "entity.fl";

MyStruct :: struct {

}

asd :: proc (c: i32) {
  d := 123;

}

global := 123;

main :: proc (a: asdasdasd, b: i32) {
  hello :: import "other.fl";

  bb := (hello.puts())();

  aa := Entity;

  other_const :: 123;
  some_const :: other_const;

  adghfdgh: u32 = 123;
  bbb: i32 = 123;


  derp :: proc () {
    aaa :: hello.puts;
    b := hello.puts();

    aaaa := derp();
    asdf := global;
    asdf = b;
  }


  aaa := derp();
}
